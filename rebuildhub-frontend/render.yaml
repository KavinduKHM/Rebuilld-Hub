services:
  - type: static_site
    name: rebuildhub-frontend
    rootDir: rebuildhub-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
